export function ContractAcceptanceModal({ 
  contract, 
  open, 
  onOpenChange, 
  onAccepted 
}: { 
  contract: any; 
  open: boolean; 
  onOpenChange: (open: boolean) => void; 
  onAccepted?: () => void;
}) {
  return null; // Stub
}
