export function WithdrawalRequestModal({ 
  open, 
  onOpenChange, 
  onSuccess,
  availableBalance,
  currency
}: { 
  open: boolean; 
  onOpenChange: (open: boolean) => void; 
  onSuccess?: () => void;
  availableBalance?: number;
  currency?: string;
}) {
  return null; // Stub
}
